# 序列问题（不要求连续）
/** 最长连续序列
面试中经常会出现这种题目，考察思维。
在O(N)时间复杂度下找到无序数组的最长连续序列
*/

/************************************** Set集合 **************************************/
*      Set<Integer> cnt = Arrays.Stream(input).boxed().collect(Collectors.toSet());
       for(int i : nums){
         if(!set.contains(i - 1)){
           int start = i;
           while(set.contains(++i)){
            set.remove(i);
           }
           ans = Math.max(ans , i - start);
       }

/** 最长公共子序列
找出两个字符串中的最长公共子序列的长度
*/

/************************************** dp状态转移 **************************************/
*     int dfs(int num1 , int num2 , int[][] dp){
         if(num1 == dp.length || num2 == dp[0].length) return 0;
         if(dp[num1][num2] == -1) {
                if(str1[num1] == str2[num2]) dp[num1][num2] = dfs(num1 + 1 , num2 + 1 , dp) + 1;
                else dp[num1][num2] = Math.max(dfs(num1 + 1 , num2 , dp) , dfs(num1 , num2 + 1 , dp));
         }
         return dp[num1][num2];
      }
         
